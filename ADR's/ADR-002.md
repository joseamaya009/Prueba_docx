
## ADR-002 — Diseño de Roles y Permisos Diferenciados para Seguridad y Trazabilidad

**Título:** Implementar una estrategia de control de acceso basada en roles (RBAC) a nivel de base de datos PostgreSQL, complementando el modelo lógico existente.

**Contexto:**
El modelo ya define las tablas `security_role`, `security_permission`, `user_account`, `user_role` y `role_permission`, lo que evidencia una intención RBAC en el diseño lógico. Sin embargo, esta estructura sólo existe como dato; no hay roles de base de datos PostgreSQL que hagan cumplir esos permisos a nivel de motor. Cualquier conexión que llegue con el rol del owner puede leer y modificar cualquier tabla sin restricción.

**Problema:**
La ausencia de roles reales en PostgreSQL implica que la seguridad del sistema depende exclusivamente de la capa de aplicación. Un error en la aplicación, un acceso directo a la base de datos, o una conexión de administración mal controlada pueden comprometer la integridad o confidencialidad de los datos. Adicionalmente, no hay trazabilidad de qué rol realizó cada operación DDL o DML.

**Decisión:**
Se crearán roles de base de datos PostgreSQL con permisos diferenciados por función:

| Rol PostgreSQL          | Propósito                        | Permisos                                              |
| ----------------------- | --------------------------------- | ----------------------------------------------------- |
| `role_app_read`       | Consultas de solo lectura         | `SELECT` en todos los esquemas                      |
| `role_app_write`      | Operaciones transaccionales       | `SELECT, INSERT, UPDATE` en tablas operativas       |
| `role_app_admin`      | Gestión de catálogos y usuarios | `SELECT, INSERT, UPDATE, DELETE` en tablas maestras |
| `role_liquibase`      | Ejecución de migraciones DDL     | `CREATE, ALTER, DROP` bajo supervisión             |
| `role_readonly_audit` | Auditoría externa o BI           | `SELECT` únicamente, sin posibilidad de escritura  |

Cada conexión de la aplicación usará el rol mínimo necesario para su función.

**Justificación técnica:**
El principio de mínimo privilegio es un requisito estándar en sistemas que manejan datos financieros y personales. PostgreSQL soporta nativamente herencia de roles y control granular por esquema y tabla. Separar el rol de Liquibase del rol de aplicación garantiza que las migraciones DDL no puedan ejecutarse desde el contexto de una petición normal. La tabla `user_role` del modelo lógico puede complementarse con columnas de auditoría como `revoked_at` y `revoked_by` para mayor trazabilidad.

**Consecuencias / Impacto esperado:**

* Reducción del riesgo de acceso indebido a datos sensibles (documentos, pagos, contraseñas).
* Las conexiones del contenedor de aplicación solo podrán ejecutar DML, no DDL.
* Se genera un script `roles_setup.sql` que forma parte del proceso de inicialización del contenedor PostgreSQL.
* Impacto en Liquibase: los changelogs incluirán `GRANT` statements como parte del proceso de migración controlada.
